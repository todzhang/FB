package ddb.dsz.plugin.taskmanager;

import ddb.dsz.plugin.multitarget.MultipleTargetPopupMenu;

public class TaskManagerPopupMenu extends MultipleTargetPopupMenu {
   public TaskManagerPopupMenu(TaskManagerWorkBench workbench, TaskManager2 manager) {
      super(workbench, manager);
   }
}

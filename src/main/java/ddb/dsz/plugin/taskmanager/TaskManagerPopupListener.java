package ddb.dsz.plugin.taskmanager;

import ddb.detach.Workbench;
import ddb.dsz.plugin.multitarget.MultipleTargetPopupListener;

public class TaskManagerPopupListener extends MultipleTargetPopupListener {
   public TaskManagerPopupListener(Workbench workbench) {
      super(workbench);
   }
}

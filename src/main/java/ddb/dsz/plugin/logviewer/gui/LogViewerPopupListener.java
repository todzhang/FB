package ddb.dsz.plugin.logviewer.gui;

import ddb.detach.TabbablePopupListener;
import ddb.detach.Workbench;

public class LogViewerPopupListener extends TabbablePopupListener {
   public LogViewerPopupListener(Workbench workbench) {
      super(workbench);
   }
}

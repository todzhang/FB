package ddb.dsz.plugin.logviewer.gui.screenlog;

import ddb.detach.TabbablePopupListener;
import ddb.detach.Workbench;

public class ScreenLogPopupListener extends TabbablePopupListener {
   public ScreenLogPopupListener(Workbench workbench) {
      super(workbench);
   }
}

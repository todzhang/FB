package ddb.dsz.plugin.logviewer.gui.screenlog;

import ddb.detach.WorkbenchPopupMenu;

public class ScreenLogPopupMenu extends WorkbenchPopupMenu {
   public ScreenLogPopupMenu(ScreenLogWorkspace workbench) {
      super(workbench);
   }
}
